
## sets
```python
# creating empty set
a = set()
# my_set[0]
# if you uncomment the above line  you will get an error
# TypeError: 'set' object does not support indexing

# add an element
my_set.add(2)

# initialize my_set
my_set = {1, 3}

# add multiple elements including arrays
# The update() method inserts the items in set2 into set1:
my_set.update([2, 3, 4])
l = [30, 40]
# adding all elements in list to the set
my_set.update(l)

# discard an element -- if element found no Error
my_set.discard(4)





```
